def plot(li1, li2):
	'''
	data = []
	for x in range(len(student_ans_li)):
	    data.append({'Students': student_usn_li[x], 'Score': ####@@@@@@@@@@@@@})

	powd = DataFrame(data)
	print powd

	# the above can be changed with this line:
	# powd = DataFrame(power_data, columns=['Students', 'Score'])
	# see sugestion in comments 

	print(ggplot(aes(x='Students', y='Score'), data=powd) + \
	    geom_point(color='lightblue') + \
	    geom_line(alpha=0.25) + \
	    stat_smooth(span=.05, color='black') + \
	    ggtitle("Accuracy Plot") + \
	    xlab("Students") + \
	    ylab("Score"))


	print(ggplot(aes(x='date', y='value', colour='variable'), data=meat_lng) + \
	    stat_smooth(span=0.10) + \
	    ggtitle("Smoothed Livestock Production"))
	'''